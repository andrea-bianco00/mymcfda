% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_data.R
\name{prepare_data}
\alias{prepare_data}
\title{Validate and convert functional sparse data to T_list / Y_list format}
\usage{
prepare_data(
  data = NULL,
  T_list = NULL,
  Y_list = NULL,
  id_col = "subject_id",
  t_col = "t",
  y_col = "Y"
)
}
\arguments{
\item{data}{Either a data frame (long format) or NULL if T_list/Y_list
are provided directly.}

\item{T_list}{List of observation times. Used only if data is NULL.}

\item{Y_list}{List of observed values. Used only if data is NULL.}

\item{id_col}{Character string. Name of the subject id column in data.
Default is "subject_id".}

\item{t_col}{Character string. Name of the time column in data.
Default is "t".}

\item{y_col}{Character string. Name of the observed value column in data.
Default is "Y".}
}
\value{
A list with components:
\itemize{
\item T_list: list of length n with observation times per subject.
\item Y_list: list of length n with observed values per subject.
}
}
\description{
Validate and convert functional sparse data to T_list / Y_list format
}
\examples{
# Format 1: long data frame
df <- data.frame(
  subject_id = c(1,1,1, 2,2,2,2, 3,3),
  t          = c(0.1, 0.2, 0.3, 0.5, 0.6, 0.7, 0.8, 0.3, 0.4),
  Y          = c(1.2, 0.9, 1.1, 0.8, 1.0, 1.2, 0.9, 1.3, 1.1)
)
result <- prepare_data(data = df)

# Format 2: already lists
T_list <- list(c(0.1, 0.2, 0.3), c(0.5, 0.6, 0.7, 0.8), c(0.3, 0.4))
Y_list <- list(c(1.2, 0.9, 1.1), c(0.8, 1.0, 1.2, 0.9), c(1.3, 1.1))
result <- prepare_data(T_list = T_list, Y_list = Y_list)
}
