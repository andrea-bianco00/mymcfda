% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_fO_truncbeta.R
\name{sample_fO_truncbeta}
\alias{sample_fO_truncbeta}
\title{Draw n i.i.d. samples from the Rescaled Beta density of f_O}
\usage{
sample_fO_truncbeta(n, delta, a_O = 2, b_O = 2)
}
\arguments{
\item{n}{Integer >= 1. Number of samples to draw.}

\item{delta}{Numeric in (0, 1). Snippet length. Determines the support
(delta/2, 1 - delta/2).}

\item{a_O}{Numeric > 0. First shape parameter of Beta(a_O, b_O).
Must be > 1 for strict positivity at the left boundary.
Default: 2.}

\item{b_O}{Numeric > 0. Second shape parameter of Beta(a_O, b_O).
Must be > 1 for strict positivity at the right boundary.
Default: 2.}
}
\value{
Numeric vector of length n with samples O_1, ..., O_n,
all in (delta/2, 1 - delta/2).
}
\description{
Draw n i.i.d. samples from the Rescaled Beta density of f_O
}
\examples{
# Default: symmetric Beta(2,2)
O <- sample_fO_truncbeta(n = 200, delta = 0.4)
hist(O, breaks = 20, main = "f_O truncbeta Beta(2,2), delta = 0.4")

# Left-skewed Beta(3,2): more subjects enter early
O2 <- sample_fO_truncbeta(n = 500, delta = 0.4, a_O = 3, b_O = 2)

# Right-skewed Beta(2,3): more subjects enter late
O3 <- sample_fO_truncbeta(n = 500, delta = 0.4, a_O = 2, b_O = 3)
}
