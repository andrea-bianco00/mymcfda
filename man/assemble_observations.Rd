% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assemble_observations.R
\name{assemble_observations}
\alias{assemble_observations}
\title{Assemble final observed data from mean, centred process, and noise}
\usage{
assemble_observations(mu_values, Z_list, noise_list)
}
\arguments{
\item{mu_values}{List of n numeric vectors. Mean values at observed times.}

\item{Z_list}{List of n numeric vectors. Centred process values.}

\item{noise_list}{List of n numeric vectors. Measurement noise values.}
}
\value{
A list with:
\itemize{
\item Y_list  : list of n numeric vectors with final observed data
\item config  : list with assembly metadata
}
}
\description{
Assemble final observed data from mean, centred process, and noise
}
\examples{
mu_values  <- list(c(1, 2, 3), c(0.5, 0.7))
Z_list     <- list(c(0.1, -0.2, 0.3), c(-0.1, 0.2))
noise_list <- list(c(0.01, -0.03, 0.02), c(0.05, -0.02))

out <- assemble_observations(mu_values, Z_list, noise_list)
out$Y_list
}
