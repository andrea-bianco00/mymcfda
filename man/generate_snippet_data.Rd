% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_snippet_data.R
\name{generate_snippet_data}
\alias{generate_snippet_data}
\title{Generate complete snippet observation structure}
\usage{
generate_snippet_data(
  n,
  m_mean = 4,
  delta = 0.25,
  seed = NULL,
  fO_type = "uniform",
  fO_params = list(),
  f0_type = "uniform",
  f0_params = list()
)
}
\arguments{
\item{n}{Integer >= 1. Number of subjects.}

\item{m_mean}{Numeric > 0. Average number of measurements per snippet.
Passed as the Poisson mean (before truncation at >= 2).
Default: 4 (as in Lin & Wang 2022).}

\item{delta}{Numeric in (0, 1). Snippet length.
Default: 0.25 (as in Lin & Wang 2022).}

\item{seed}{Integer or NULL. Random seed for reproducibility.
If NULL, no seed is set. Default: NULL.}

\item{fO_type}{Character. Density family for reference times O_i.
One of: "uniform" (default), "truncnorm", "truncbeta",
"truncexp".}

\item{fO_params}{Named list. Parameters for the chosen f_O family.
See sample_fO.R for documentation of each family.
Default: list() (uses each family's defaults).}

\item{f0_type}{Character. Density family for measurement times T_ij.
One of: "uniform" (default), "truncnorm", "truncbeta",
"truncexp".}

\item{f0_params}{Named list. Parameters for the chosen f_0 family.
See sample_fzero.R for documentation of each family.
Default: list() (uses each family's defaults).}
}
\value{
A list with components:
\itemize{
\item T_list    : list of n numeric vectors. T_list((i)) contains the m_i
measurement times for subject i, sorted in increasing
order, all in (0, 1).
\item m_vec     : integer vector of length n. Number of measurements per
subject (m_1, ..., m_n), all >= 2.
\item O_vec     : numeric vector of length n. Reference times O_1, ..., O_n
in (delta/2, 1 - delta/2). LATENT in real data.
\item windows   : data frame with columns A (left endpoint) and B (right
endpoint) of each subject's observation window. LATENT.
\item config    : list containing all input parameters (n, m_mean, delta,
seed, fO_type, fO_params, f0_type, f0_params) for full
reproducibility.
\item summary   : list with quick diagnostics:
\itemize{
\item n              : number of subjects
\item m_mean_actual  : actual sample mean of m_vec
\item m_range        : range of m_vec (min, max)
\item total_obs      : total number of observations sum(m_vec)
\item snippet_widths : summary stats of snippet widths (min, mean, max)
}
}
}
\description{
Generate complete snippet observation structure
}
\examples{
# === Example 1: Default (as in Lin & Wang 2022) ===
dat <- generate_snippet_data(n = 200, m_mean = 4, delta = 0.25, seed = 42)
str(dat$T_list[1:3])        # first 3 subjects' measurement times
dat$summary                  # quick diagnostics

# === Example 2: Truncated Normal for both O_i and T_ij ===
dat2 <- generate_snippet_data(
  n         = 100,
  m_mean    = 6,
  delta     = 0.30,
  seed      = 123,
  fO_type   = "truncnorm",
  fO_params = list(mu_O = 0.5, sd_O = 0.15),
  f0_type   = "truncnorm",
  f0_params = list(mu_0 = 0.15, sd_0 = 0.06)
)

# === Example 3: Beta reference times, Exponential measurement times ===
dat3 <- generate_snippet_data(
  n         = 50,
  m_mean    = 3,
  delta     = 0.40,
  seed      = 7,
  fO_type   = "truncbeta",
  fO_params = list(a_O = 2, b_O = 5),
  f0_type   = "truncexp",
  f0_params = list(lambda = 3)
)
}
