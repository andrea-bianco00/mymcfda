% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density_fO_truncbeta.R
\name{fO_truncbeta}
\alias{fO_truncbeta}
\title{Rescaled Beta density for reference times f_O}
\usage{
fO_truncbeta(u, delta, a_O = 2, b_O = 2)
}
\arguments{
\item{u}{Numeric vector. Points at which to evaluate f_O.
Values outside (delta/2, 1 - delta/2) return 0.}

\item{delta}{Numeric in (0, 1). Snippet length. Determines the support
(delta/2, 1 - delta/2).}

\item{a_O}{Numeric > 0. First shape parameter of Beta(a_O, b_O).
Must be > 1 for strict positivity at the left boundary.
Default: 2.}

\item{b_O}{Numeric > 0. Second shape parameter of Beta(a_O, b_O).
Must be > 1 for strict positivity at the right boundary.
Default: 2.}
}
\value{
Numeric vector of the same length as u with values of f_O(u).
}
\description{
Rescaled Beta density for reference times f_O
}
\examples{
u <- seq(0, 1, length.out = 500)

# Symmetric Beta(2,2) — default
f <- fO_truncbeta(u, delta = 0.4)
plot(u, f, type = "l", main = "f_O truncbeta, delta = 0.4")

# Left-skewed Beta(3,2): more subjects enter early
f2 <- fO_truncbeta(u, delta = 0.4, a_O = 3, b_O = 2)
lines(u, f2, col = "red")

# Right-skewed Beta(2,3): more subjects enter late
f3 <- fO_truncbeta(u, delta = 0.4, a_O = 2, b_O = 3)
lines(u, f3, col = "blue")

# Verify it integrates to 1
sum(f) * (1 / 500)   # approximately 1
}
