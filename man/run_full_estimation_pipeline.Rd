% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_full_estimation_pipeline.R
\name{run_full_estimation_pipeline}
\alias{run_full_estimation_pipeline}
\title{Run the full estimation pipeline}
\usage{
run_full_estimation_pipeline(
  T_list,
  Y_list,
  kernel = "epanechnikov",
  scheme = "OBS",
  kappa = 5,
  n_h = 20,
  n_grid = 100,
  model = "power_exponential",
  h_mu = NULL,
  h_sigma = NULL,
  theta_init = NULL,
  s_grid = NULL,
  t_grid = NULL,
  optim_method = "BFGS",
  optim_control = NULL,
  seed_global = NULL,
  seed_cv_mu = NULL,
  seed_cv_sigma = NULL,
  return_all = TRUE,
  run_checks = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{T_list}{List of observation times per subject.}

\item{Y_list}{List of observed values per subject.}

\item{kernel}{Character. Kernel type. Default "epanechnikov".}

\item{scheme}{Character. Weighting scheme. Default "OBS".}

\item{kappa}{Integer >= 2. Number of CV folds. Default 5.}

\item{n_h}{Integer >= 2. Number of bandwidth candidates. Default 20.}

\item{n_grid}{Integer >= 2. Number of grid points for function estimates.
Default 100.}

\item{model}{Character. Correlation model for covariance estimation.
One of "power_exponential", "rational_quadratic", "matern".}

\item{h_mu}{Optional numeric scalar. If provided, skip CV for mu.}

\item{h_sigma}{Optional numeric scalar. If provided, skip CV for varsigma^2.}

\item{theta_init}{Optional numeric vector of length 2. Initial value for theta.}

\item{s_grid}{Optional numeric vector. Grid for covariance surface.}

\item{t_grid}{Optional numeric vector. Grid for covariance surface.}

\item{optim_method}{Character. Optimization method for estimate_theta().
Default "BFGS".}

\item{optim_control}{Optional list of control parameters for optim().}

\item{seed_global}{Optional integer. Global seed set at the start.}

\item{seed_cv_mu}{Optional integer. Seed for CV of mu. If NULL, uses
seed_global when available.}

\item{seed_cv_sigma}{Optional integer. Seed for CV of varsigma^2. If NULL,
uses seed_global + 1 when available.}

\item{return_all}{Logical. If TRUE, return the full pipeline output.
If FALSE, return a compact output.}

\item{run_checks}{Logical. If TRUE, run data checks and reorder each
subject by increasing time if needed.}

\item{verbose}{Logical. If TRUE, print progress information.}
}
\value{
If return_all = TRUE, a list with:
\itemize{
\item inputs
\item seeds
\item cv_mu
\item h_mu
\item res_mu
\item mu_obs_list
\item Z_list
\item cv_sigma
\item h_sigma
\item res_varsigma
\item h0_res
\item sigma0_res
\item res_sigmaX2
\item covariance_fit
\item diagnostics
}

If return_all = FALSE, a compact list with:
\itemize{
\item h_mu
\item h_sigma
\item sigma0_hat
\item theta_hat
\item covariance_hat
\item diagnostics
}
}
\description{
Run the full estimation pipeline
}
\examples{
# fit <- run_full_estimation_pipeline(
#   T_list = T_list,
#   Y_list = Y_list,
#   model  = "power_exponential",
#   seed_global = 123,
#   verbose = TRUE
# )
}
