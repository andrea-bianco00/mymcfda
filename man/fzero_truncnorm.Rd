% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density_fzero_truncnorm.R
\name{fzero_truncnorm}
\alias{fzero_truncnorm}
\title{Truncated Normal density for measurement times f_0}
\usage{
fzero_truncnorm(s, delta, mu_0 = NULL, sd_0 = NULL)
}
\arguments{
\item{s}{Numeric vector. Points at which to evaluate f_0.
Values outside (0, delta) return 0.}

\item{delta}{Numeric in (0, 1). Snippet length. Determines the support
(0, delta).}

\item{mu_0}{Numeric. Centre of the normal distribution on (0, delta).
Default: delta/2 (midpoint of the window).}

\item{sd_0}{Numeric > 0. Scale of the normal distribution.
Default: delta/4, placing ~95\% of the mass inside (0, delta).}
}
\value{
Numeric vector of the same length as s with values of f_0(s).
}
\description{
Truncated Normal density for measurement times f_0
}
\examples{
s <- seq(-0.1, 0.6, length.out = 500)

# Default: centred at delta/2 = 0.2
f <- fzero_truncnorm(s, delta = 0.4)
plot(s, f, type = "l", main = "f_0 truncnorm, delta = 0.4")

# Shifted towards the end of the window
f2 <- fzero_truncnorm(s, delta = 0.4, mu_0 = 0.3, sd_0 = 0.08)
lines(s, f2, col = "red")

# Verify it integrates to 1
s_fine <- seq(0, 0.4, length.out = 10000)
sum(fzero_truncnorm(s_fine, delta = 0.4)) * (0.4 / 10000)  # approximately 1
}
