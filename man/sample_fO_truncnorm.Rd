% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_fO_truncnorm.R
\name{sample_fO_truncnorm}
\alias{sample_fO_truncnorm}
\title{Draw n i.i.d. samples from the Truncated Normal density of f_O}
\usage{
sample_fO_truncnorm(n, delta, mu_O = NULL, sd_O = NULL)
}
\arguments{
\item{n}{Integer >= 1. Number of samples to draw.}

\item{delta}{Numeric in (0, 1). Snippet length. Determines the support
(delta/2, 1 - delta/2).}

\item{mu_O}{Numeric. Centre of the normal distribution. Default is the
midpoint of the support: 0.5.}

\item{sd_O}{Numeric > 0. Scale of the normal distribution. Default is
(1 - delta) / 4, placing ~95\% of the normal mass inside
the support.}
}
\value{
Numeric vector of length n with samples O_1, ..., O_n,
all in (delta/2, 1 - delta/2).
}
\description{
Draw n i.i.d. samples from the Truncated Normal density of f_O
}
\examples{
# Default: centred at 0.5
O <- sample_fO_truncnorm(n = 200, delta = 0.4)
hist(O, breaks = 20, main = "f_O truncnorm, delta = 0.4")

# Shifted towards 0.7: more subjects enter late
O2 <- sample_fO_truncnorm(n = 500, delta = 0.4, mu_O = 0.7, sd_O = 0.1)
}
