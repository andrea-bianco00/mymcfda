% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density_fO_truncnorm.R
\name{fO_truncnorm}
\alias{fO_truncnorm}
\title{Truncated Normal density for reference times f_O}
\usage{
fO_truncnorm(u, delta, mu_O = NULL, sd_O = NULL)
}
\arguments{
\item{u}{Numeric vector. Points at which to evaluate f_O.
Values outside (delta/2, 1 - delta/2) return 0.}

\item{delta}{Numeric in (0, 1). Snippet length. Determines the support
(delta/2, 1 - delta/2).}

\item{mu_O}{Numeric. Centre of the normal distribution. Default is the
midpoint of the support: (delta/2 + 1 - delta/2) / 2 = 0.5.}

\item{sd_O}{Numeric > 0. Scale of the normal distribution. Default is
(1 - delta) / 4, i.e. one quarter of the support length,
which places approximately 95\% of the mass inside the support.}
}
\value{
Numeric vector of the same length as u with values of f_O(u).
}
\description{
Truncated Normal density for reference times f_O
}
\examples{
u <- seq(0, 1, length.out = 500)

# Centred at 0.5
f <- fO_truncnorm(u, delta = 0.4, mu_O = 0.5, sd_O = 0.15)
plot(u, f, type = "l", main = "f_O truncnorm, delta = 0.4")

# Shifted towards 0.7 (more subjects enter late)
f2 <- fO_truncnorm(u, delta = 0.4, mu_O = 0.7, sd_O = 0.1)
lines(u, f2, col = "red")

# Verify it integrates to 1
sum(f) * (1 / 500)   # approximately 1
}
