% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mu_hat.R
\name{compute_mu_hat}
\alias{compute_mu_hat}
\title{Ridged local linear estimator of mu(t) on a grid of points}
\usage{
compute_mu_hat(T_list, Y_list, h_mu, kernel, scheme, t_grid = NULL, n_t = 100)
}
\arguments{
\item{T_list}{List of length n. T_list((i)) is the vector of observation
times for subject i.}

\item{Y_list}{List of length n. Y_list((i)) is the vector of observed
values Y_ij for subject i.}

\item{h_mu}{Numeric scalar. Bandwidth h_mu > 0.}

\item{kernel}{Character string. Kernel type, passed to compute_mu_hat_t().}

\item{scheme}{Character string. Weighting scheme. One of "OBS", "SUBJ", "OPTIMAL".}

\item{t_grid}{Numeric vector. Grid of points at which mu(t) is estimated.
If NULL (default), built automatically as n_t equally spaced
points over (min(T_ij), max(T_ij)).}

\item{n_t}{Positive integer. Number of grid points when t_grid is built
automatically. Default is 100.}
}
\value{
A list with:
$t_grid:  Numeric vector. The grid of evaluation points.
$mu_hat:  Numeric vector. Estimated mu_hat(t) for each t in t_grid.
}
\description{
Ridged local linear estimator of mu(t) on a grid of points
}
