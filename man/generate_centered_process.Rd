% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_centered_process.R
\name{generate_centered_process}
\alias{generate_centered_process}
\title{Generate centred process through KL or decomposition road}
\usage{
generate_centered_process(
  T_list,
  road,
  eigenfn_list = NULL,
  eigenval_vec = NULL,
  score_type = "gaussian",
  score_params = list(),
  sigmaX_fn = NULL,
  corr_type = NULL,
  corr_params = list(),
  jitter = 1e-10,
  seed = NULL
)
}
\arguments{
\item{T_list}{List of n numeric vectors. T_list((i)) contains the m_i
measurement times for subject i.}

\item{road}{Character. Must be either "kl" or "decomposition".}

\item{eigenfn_list}{List of K vectorised eigenfunctions. Used only if
road = "kl".}

\item{eigenval_vec}{Numeric vector of K positive eigenvalues. Used only if
road = "kl".}

\item{score_type}{Character. Distribution family for KL scores.
Used only if road = "kl". Default: "gaussian".}

\item{score_params}{Named list of parameters for score distribution.
Used only if road = "kl". Default: list().}

\item{sigmaX_fn}{Function. Standard deviation function sigma_X(t).
Used only if road = "decomposition".}

\item{corr_type}{Character. Correlation family. Used only if
road = "decomposition".}

\item{corr_params}{Named list of correlation parameters. Used only if
road = "decomposition". Default: list().}

\item{jitter}{Small non-negative number added to the diagonal of
Sigma_i for numerical stability. Used only if
road = "decomposition". Default: 1e-10.}

\item{seed}{Integer or NULL. Optional seed for reproducibility.}
}
\value{
A list. Structure depends on the chosen road:
\itemize{
\item road = "kl":
list(
Z_list = ...,
scores = ...
)
\item road = "decomposition":
list(
Z_list = ...,
Sigma_list = ...,
config = ...
)
}
}
\description{
Generate centred process through KL or decomposition road
}
\examples{
# ---------------------------------------------------------------------------
# Example 1: KL road
# ---------------------------------------------------------------------------
T_list <- list(c(0.1, 0.2, 0.3), c(0.55, 0.60, 0.72, 0.80))

K <- 10
eigenfn_list <- lapply(1:K, function(k) {
  force(k)
  function(t) sqrt(2) * sin(2 * k * pi * t)
})
eigenval_vec <- 1 / (1:K)^2

out_kl <- generate_centered_process(
  T_list = T_list,
  road = "kl",
  eigenfn_list = eigenfn_list,
  eigenval_vec = eigenval_vec,
  score_type = "gaussian",
  seed = 123
)

# ---------------------------------------------------------------------------
# Example 2: Decomposition road
# ---------------------------------------------------------------------------
sigmaX_fn <- function(t) {
  sqrt( sqrt(t) * sqrt(1 - t) / 10 + 1 )
}

out_dec <- generate_centered_process(
  T_list = T_list,
  road = "decomposition",
  sigmaX_fn = sigmaX_fn,
  corr_type = "matern",
  corr_params = list(theta1 = 0.5, theta2 = 1),
  seed = 123
)
}
