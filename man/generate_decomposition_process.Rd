% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_decomposition_process.R
\name{generate_decomposition_process}
\alias{generate_decomposition_process}
\title{Generate centred process via variance-correlation decomposition}
\usage{
generate_decomposition_process(
  T_list,
  sigmaX_fn,
  corr_type,
  corr_params = list(),
  seed = NULL,
  return_Sigma = TRUE,
  jitter = 1e-10
)
}
\arguments{
\item{T_list}{List of n numeric vectors. T_list((i)) contains the m_i
measurement times for subject i.}

\item{sigmaX_fn}{Function. Standard deviation function sigma_X(t).
Must be vectorised and non-negative.}

\item{corr_type}{Character. Correlation family. One of:
"matern"
"power_exponential"
"rational_quadratic"}

\item{corr_params}{Named list of parameters for the chosen correlation.}

\item{seed}{Integer or NULL. Optional seed for reproducibility.}

\item{return_Sigma}{Logical. If TRUE, also returns Sigma_list.
Default: TRUE.}

\item{jitter}{Small non-negative number added to the diagonal of
Sigma_i for numerical stability. Default: 1e-10.}
}
\value{
A list with:
\itemize{
\item Z_list     : list of n numeric vectors. Z_list((i)) contains
Z_i(T_i1), ..., Z_i(T_im_i).
\item Sigma_list : optional list of subject-specific covariance matrices.
\item config     : list of arguments used for generation.
}
}
\description{
Generate centred process via variance-correlation decomposition
}
\examples{
T_list <- list(c(0.1, 0.2, 0.3), c(0.55, 0.60, 0.72, 0.80))

sigmaX_fn <- function(t) {
  sqrt( sqrt(t) * sqrt(1 - t) / 10 + 1 )
}

out <- generate_decomposition_process(
  T_list = T_list,
  sigmaX_fn = sigmaX_fn,
  corr_type = "matern",
  corr_params = list(theta1 = 0.5, theta2 = 1),
  seed = 123
)

str(out$Z_list)
str(out$Sigma_list)
}
