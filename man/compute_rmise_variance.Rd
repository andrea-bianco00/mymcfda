% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_rmise_variance.R
\name{compute_rmise_variance}
\alias{compute_rmise_variance}
\title{Compute RMISE for sigma_X^2(t)}
\usage{
compute_rmise_variance(
  estimates_list,
  t_grid,
  sigmaX2_true = NULL,
  sigmaX2_true_fun = NULL
)
}
\arguments{
\item{estimates_list}{List of length N. Each element is either:
- a numeric vector giving sigmaX2_hat on t_grid, or
- a list containing component $sigmaX2_hat.}

\item{t_grid}{Numeric vector. Common evaluation grid.}

\item{sigmaX2_true}{Optional numeric vector. True sigma_X^2(t) on t_grid.}

\item{sigmaX2_true_fun}{Optional function. True sigma_X^2(t), evaluated on t_grid.

Exactly one of sigmaX2_true or sigmaX2_true_fun must be provided.}
}
\value{
Numeric scalar. The RMISE value.
}
\description{
Compute RMISE for sigma_X^2(t)
}
\examples{
t_grid <- seq(0, 1, length.out = 100)
sigmaX2_true_fun <- function(t) 0.2 + 0.1 * sin(2*pi*t)

est1 <- sigmaX2_true_fun(t_grid) + rnorm(100, 0, 0.01)
est2 <- sigmaX2_true_fun(t_grid) + rnorm(100, 0, 0.02)

compute_rmise_variance(
  estimates_list   = list(est1, est2),
  t_grid           = t_grid,
  sigmaX2_true_fun = sigmaX2_true_fun
)
}
