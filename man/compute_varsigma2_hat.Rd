% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/varsigma2_hat.R
\name{compute_varsigma2_hat}
\alias{compute_varsigma2_hat}
\title{Ridged local linear estimator of varsigma^2(t) on a grid of points}
\usage{
compute_varsigma2_hat(
  T_list,
  Z_list,
  h_sigma,
  kernel,
  scheme,
  t_grid = NULL,
  n_t = 100,
  truncate_nonnegative = TRUE
)
}
\arguments{
\item{T_list}{List of length n. T_list((i)) is the vector of
observation times for subject i.}

\item{Z_list}{List of length n. Z_list((i)) is the vector of
squared residuals Z_ij = {Y_ij - mu_hat(T_ij)}^2
for subject i.}

\item{h_sigma}{Numeric scalar. Bandwidth h_sigma > 0.}

\item{kernel}{Character string. Kernel type, passed to
compute_varsigma2_hat_t().}

\item{scheme}{Character string. Weighting scheme.
One of "OBS", "SUBJ", "OPTIMAL".}

\item{t_grid}{Numeric vector. Grid of points at which
varsigma^2(t) is estimated. If NULL (default),
built automatically as n_t equally spaced points
over (min(T_ij), max(T_ij)).}

\item{n_t}{Positive integer. Number of grid points when
t_grid is built automatically. Default is 100.}

\item{truncate_nonnegative}{Logical. If TRUE (default), truncate negative
estimates to 0.}
}
\value{
A list with:
$t_grid:        Numeric vector. The grid of evaluation points.
$varsigma2_hat: Numeric vector. Estimated varsigma2_hat(t) for each t in t_grid.
}
\description{
Ridged local linear estimator of varsigma^2(t) on a grid of points
}
