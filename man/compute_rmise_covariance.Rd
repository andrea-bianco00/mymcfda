% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_rmise_covariance.R
\name{compute_rmise_covariance}
\alias{compute_rmise_covariance}
\title{Compute RMISE for the covariance function C(s,t)}
\usage{
compute_rmise_covariance(
  estimates_list,
  s_grid,
  t_grid,
  C_true_mat = NULL,
  C_true_fun = NULL
)
}
\arguments{
\item{estimates_list}{List of length N. Each element is either:
- a matrix giving C_hat(s,t) on s_grid x t_grid, or
- a list containing component $covariance_hat.}

\item{s_grid}{Numeric vector. Grid for the first argument s.}

\item{t_grid}{Numeric vector. Grid for the second argument t.}

\item{C_true_mat}{Optional matrix. True covariance on s_grid x t_grid.}

\item{C_true_fun}{Optional function of two arguments (s, t), returning
the true covariance matrix on the grid.

Exactly one of C_true_mat or C_true_fun must be provided.}
}
\value{
Numeric scalar. The RMISE value.
}
\description{
Compute RMISE for the covariance function C(s,t)
}
\examples{
s_grid <- seq(0, 1, length.out = 50)
t_grid <- seq(0, 1, length.out = 50)

C_true_fun <- function(s, t) {
  outer(s, t, function(x, y) exp(-abs(x - y)))
}

C1 <- C_true_fun(s_grid, t_grid) + matrix(rnorm(50 * 50, 0, 0.01), 50, 50)
C2 <- C_true_fun(s_grid, t_grid) + matrix(rnorm(50 * 50, 0, 0.02), 50, 50)

compute_rmise_covariance(
  estimates_list = list(C1, C2),
  s_grid         = s_grid,
  t_grid         = t_grid,
  C_true_fun     = C_true_fun
)
}
