% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_full_snippet_data.R
\name{generate_full_snippet_data}
\alias{generate_full_snippet_data}
\title{Generate full functional snippet data}
\usage{
generate_full_snippet_data(
  n,
  m_mean = 4,
  delta = 0.25,
  seed = NULL,
  seed_list = NULL,
  fO_type = "uniform",
  fO_params = list(),
  f0_type = "uniform",
  f0_params = list(),
  mu_fn = function(t) 2 * t^2 * cos(2 * pi * t),
  road,
  eigenfn_list = NULL,
  eigenval_vec = NULL,
  score_type = "gaussian",
  score_params = list(),
  sigmaX_fn = NULL,
  corr_type = NULL,
  corr_params = list(),
  jitter = 1e-10,
  noise_structure = c("homoscedastic", "heteroscedastic"),
  sigma0 = 0,
  noise_mean = 0,
  noise_type = "gaussian",
  noise_params = list()
)
}
\arguments{
\item{n}{Integer >= 1. Number of subjects.}

\item{m_mean}{Numeric > 0. Mean of truncated Poisson for m_i.
Default: 4.}

\item{delta}{Numeric in (0,1). Snippet length. Default: 0.25.}

\item{seed}{Integer or NULL. Simple global seed. Default: NULL.}

\item{seed_list}{Named list of optional block-specific seeds. Possible
names:
- design
- process
- noise
If provided, it has priority over seed.
Default: NULL.}

\item{fO_type}{Character. Density family for O_i. Default: "uniform".}

\item{fO_params}{Named list. Parameters for f_O. Default: list().}

\item{f0_type}{Character. Density family for within-window times.
Default: "uniform".}

\item{f0_params}{Named list. Parameters for f_0. Default: list().}

\item{mu_fn}{Vectorised mean function mu(t).
Default: 2\emph{t^2}cos(2\emph{pi}t).}

\item{road}{Character. Must be "kl" or "decomposition".}

\item{eigenfn_list}{List of eigenfunctions, used only if road = "kl".}

\item{eigenval_vec}{Numeric vector of eigenvalues, used only if road = "kl".}

\item{score_type}{Character. Score distribution for KL road.
Default: "gaussian".}

\item{score_params}{Named list. Score parameters. Default: list().}

\item{sigmaX_fn}{Function sigma_X(t), used only if
road = "decomposition".}

\item{corr_type}{Character. Correlation family, used only if
road = "decomposition".}

\item{corr_params}{Named list. Correlation parameters, used only if
road = "decomposition". Default: list().}

\item{jitter}{Small non-negative diagonal inflation for decomposition
road. Default: 1e-10.}

\item{noise_structure}{Character. Must be either "homoscedastic" or
"heteroscedastic".
- "homoscedastic"  -> sigma0 must be numeric
- "heteroscedastic"-> sigma0 must be a function
Default: "homoscedastic".}

\item{sigma0}{If noise_structure = "homoscedastic", sigma0 must be
a single non-negative numeric value.
If noise_structure = "heteroscedastic", sigma0 must be
a vectorised function sigma0(t).
Default: 0.}

\item{noise_mean}{Numeric. Must be 0. Default: 0.}

\item{noise_type}{Character. Noise distribution. Default: "gaussian".}

\item{noise_params}{Named list. Noise parameters. Default: list().}
}
\value{
A list with:
\itemize{
\item Y_list
\item T_list
\item mu_values
\item Z_list
\item noise_list
\item m_vec
\item O_vec
\item windows
\item process_info
\item design_info
\item config
\item summary
}
}
\description{
Generate full functional snippet data
}
